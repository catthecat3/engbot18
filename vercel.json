{
  "builds": [
    {
      "src": "api/bot.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/bot.py"
    }
  ]
}
```

**3. Структура папок:**
```
ваш-проект/
├── api/
│   └── bot.py    (код бота из артефакта выше)
├── vercel.json
└── requirements.txt
```

## 🚀 Деплой на Vercel

1. Загрузите файл PDF `PROMT.pdf` на GitHub (в репозиторий) или любое облако
2. В коде замените `PROMPTS_PDF_URL` на прямую ссылку к файлу
3. Загрузите проект на GitHub
4. Зайдите на [vercel.com](https://vercel.com)
5. Import Project → выберите ваш репозиторий
6. Добавьте Environment Variable:
   - **Name:** `BOT_TOKEN`
   - **Value:** ваш токен от BotFather
7. Deploy!

## ⚙️ Настройка webhook

После деплоя получите URL (например: `https://ваш-проект.vercel.app`)

Установите webhook командой (в браузере):
```
https://api.telegram.org/bot<ВАШ_ТОКЕН>/setWebhook?url=https://ваш-проект.vercel.app
